<table class="table table-bordered genre_seach_table">
	<thead>
		<tr class="table-head">
			<th class="genre_seach_head">ジャンル検索</th>
		</tr>
	</thead>
	<tbody>
			<% genres.each do |genre| %>
			<tr>
        <td class="table-body">
						<%= link_to genre.name, products_path(genre_id: genre.id),class:"genre_seach_name" %>
				 </td>
			</tr>
			<% end %>
	</tbody>
</table>